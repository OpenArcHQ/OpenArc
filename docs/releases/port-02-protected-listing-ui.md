# PORT02 protected listing editor

September12, 2026; integration base0d1e5cf. Exact OpenCode CLI DeepSeek
opencode-go/deepseek-v4.1-flash implemented the17-file owned packet.

Account/tenant-aware provider listing roots, immutable draft/version creation,
bounded history, approved publication, pause/retire and original-mutation status
recovery are integrated. Listing capability and build flag are independent of
legacy tenant writes; three writer roles and read-only operator/viewer are
preserved. No moderator/self-approval, payment or purchase UI is added.

Review closed: total headers-and-body request deadlines, exact original
operation/mutation/resource binding, input/page bounds, truthful history-load
failure and tracked capability abort on sensitive lifecycle transitions.
Same-task protected-content clearing and no-storage behavior remain tested.
Lead inspected two1280x900 screenshots; native read-only reviewer accepted the
two controller closures. Screenshots are synthetic contract-fixture QA, not a
live endpoint or production proof.

Final focused88 tests, all409 web unit tests,27 Chromium/WebKit browser cases
pass. Application types, test types, lint and build exit0. Browser suite uses
explicit synthetic HTTP fixtures. Actual production API/PostgreSQL/nginx browser
acceptance and combined final release gate remain separate required gates.
