# PORT02 marketplace production proxy

September12, 2026. Exact OpenCode CLI DeepSeek opencode-go/deepseek-v4.1-flash
implemented three independent build flags and18 allowlisted route tuples,
separate unconditional marketplace capabilities and deny-only fallbacks.

Bounded read-only review closed five initial defects: canonical typed ID
prefixes, independently installed protected GET/POST params, encoded-safe
literal query delimiter rejection, lookalike SPA fallthrough, and explicit
pre-proxy credential/header denial. Existing legacy family transport remains
unchanged; marketplace adds no-Set-Cookie/no-CORS response suppression.

Final static focused23/23 and combined deployment98/98 guards pass. Review
closure accepted. These are static source checks only: actual nginx syntax,
flag combinations and production route behavior are still required next.
No hosted deployment or full release acceptance is claimed here.
