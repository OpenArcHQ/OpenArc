# Account access disabled. No proxy, no upstream reference: every /v2/auth/
# request is a dedicated fail-closed 404, including the bare prefix.

location = /v2/auth { return 404; }
location /v2/auth/ { return 404; }
