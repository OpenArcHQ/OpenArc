# Protected organization reads disabled. No proxy and no upstream reference:
# every /v1/operator/organizations request is a dedicated fail-closed 404.

location = /v1/operator { return 404; }
location /v1/operator/ { return 404; }
